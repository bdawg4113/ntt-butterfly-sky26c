# Makefile_rom -- block-level test for the twiddle factor ROM
SIM ?= icarus
TOPLEVEL_LANG ?= verilog
VERILOG_SOURCES = $(PWD)/../src/twiddle_rom.v
TOPLEVEL = twiddle_rom
COCOTB_TEST_MODULES = test_twiddle_rom

include $(shell cocotb-config --makefiles)/Makefile.sim
