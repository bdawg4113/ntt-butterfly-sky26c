# Makefile_mult -- the three-stage pipelined Barrett multiplier
SIM ?= icarus
TOPLEVEL_LANG ?= verilog
SRC_DIR = $(PWD)/../src
SIM_BUILD = sim_build/mult
VERILOG_SOURCES = $(SRC_DIR)/mod_mult.v
TOPLEVEL = mod_mult
COCOTB_TEST_MODULES = test_mod_mult
include $(shell cocotb-config --makefiles)/Makefile.sim
